/*
 * TextComg Messages
 *
 * This contains all the text for the TextComg component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TextComg';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: '1',
  },
  tail: {
    id: `${scope}.header`,
    defaultMessage: '3!',
  },
});
